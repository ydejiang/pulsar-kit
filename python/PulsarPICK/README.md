# PulsarPICK
The `PulsarPICK`  package is designed for processing `accelsearch` results files from the `PulsaR Exploration and Search TOolkit (PRESTO)`.

First, set the corresponding file paths and candidate sifting criteria, then execute the command line below. Modify details in the files if necessary.

That is, in your terminal:
```
bash ACCEL_sift.bash
bash /path/to/code/ACCEL_sift.bash
or
bash PCSSP_sift.bash
bash /path/to/code/PCSSP_sift.bash
```


<p align="center">
  <img src="./ACCEL_sift/Pms3.7222-M13_20181006_s1_1-140fits_DM30.10_red.dat_JERK_Cand_7.pfd.png" alt="脉冲星候选体7 PFD图 (观测日期：20181006，DM=30.10)" width="700">
</p>
